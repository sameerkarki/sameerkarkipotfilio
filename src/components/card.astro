---

const {Title, preview,img,link } = Astro.props

---

<div class="card max-w-[300px] max-h-40 flex flex-col gap-4 ">
<img src={img}  alt="this is a image" class="max-h-48 ">
<a href={link} class="text-xl text-black font-bold">{Title}</a>
<p class="text-gray-600 text-sm border-b-2 pb-4 border-gray-300">{preview}</p>
</div>